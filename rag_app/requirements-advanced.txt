# Advanced RAG Application Requirements
# Install these only if you need advanced features like embeddings, LLMs, etc.

# Core dependencies (install requirements.txt first)
-r requirements.txt

# Text processing (advanced)
spacy>=3.4.0

# Vector databases and embeddings (requires torch)
torch>=1.11.0
sentence-transformers>=2.2.0
chromadb>=0.4.0
pinecone-client>=2.2.0

# LLM and AI
openai>=1.0.0
anthropic>=0.3.0
langchain>=0.1.0
langchain-community>=0.0.10

# Web framework
fastapi>=0.100.0
uvicorn>=0.23.0
streamlit>=1.25.0

# Database (for PostgreSQL support)
psycopg2-binary>=2.9.0
