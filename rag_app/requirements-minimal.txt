# Minimal requirements for basic content categorization

# Core dependencies (essential)
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Utilities
tqdm>=4.65.0

# Note: sqlite3 is built-in with Python
# Note: matplotlib and seaborn are optional for analytics
