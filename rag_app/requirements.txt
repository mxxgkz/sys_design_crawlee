# RAG Application Requirements

# Core dependencies (essential for categorization)
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Text processing (lightweight)
nltk>=3.7

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
tqdm>=4.65.0

# Visualization (optional)
matplotlib>=3.5.0
seaborn>=0.11.0

# Note: sqlite3 is built-in with Python
# Note: Heavy ML dependencies (torch, sentence-transformers, etc.) are optional for now
# Note: LLM dependencies (openai, anthropic, langchain) are optional for now
# Note: Vector database dependencies (chromadb, pinecone) are optional for now
